{
  "name": "node-db-app",
  "description": "A professional Node.js application configured with DATABASE_URL and DB_NAME for database connectivity on Heroku.",
  "repository": "https://github.com/your-username/your-repo-name",
  "logo": "https://node-js-active-record.herokuapp.com/assets/heroku-logo.png",
  "keywords": [
    "node",
    "express",
    "heroku",
    "database",
    "postgres",
    "mongodb"
  ],
  "env": {
    "DATABASE_URL": {
      "description": "The connection string for the database (e.g. postgres://user:pass@host:port/db or mongodb+srv://...)",
      "required": true
    },
    "DB_NAME": {
      "description": "The specific database name to connect to",
      "value": "my_application_db",
      "required": true
    },
    "NODE_ENV": {
      "description": "The environment mode for running the application",
      "value": "production"
    }
  },
  "formation": {
    "web": {
      "quantity": 1,
      "size": "eco"
    }
  },
  "addons": [
    {
      "plan": "heroku-postgresql:essential-0"
    }
  ],
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ]
}